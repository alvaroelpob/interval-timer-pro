import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    timerstate: {
        fontSize: 30,
        alignSelf: 'center'
    },
    countdown: {
        fontSize: 90,
        marginTop: -17,
        alignSelf: 'center'
    }
});

export default styles;